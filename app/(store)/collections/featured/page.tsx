import type {Metadata} from "next";import {ProductGrid} from "@/components/product-grid";import {getCategories,getProducts} from "@/lib/data";
export const metadata:Metadata={title:"مختاراتنا",description:"مختارات نوفا مودا المميزة من العبايات."};
export default async function FeaturedCollection(){const[products,categories]=await Promise.all([getProducts({featured:true}),getCategories()]);return <section className="py-10 sm:py-14"><div className="container-shell"><div className="mb-9 max-w-2xl"><p className="text-[10px] font-extrabold tracking-[.22em] text-[var(--brand-strong)]">NOVA PICKS</p><h1 className="mt-3 text-[40px] font-extrabold sm:text-[54px]">مختاراتنا</h1><p className="mt-3 text-sm leading-7 text-[var(--muted)]">قطع مميزة اختارتها نوفا مودا للواجهة الرئيسية.</p></div><ProductGrid products={products} categories={categories}/></div></section>}
