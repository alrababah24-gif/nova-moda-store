import type {Metadata} from "next";import {notFound} from "next/navigation";import {ProductGrid} from "@/components/product-grid";import {getCategories,getProducts} from "@/lib/data";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const c=(await getCategories()).find(x=>x.slug===slug);return c?{title:c.name,description:c.description||`تسوّقي ${c.name} من نوفا مودا`}:{title:"المجموعة غير موجودة"}}
export default async function CategoryCollection({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const categories=await getCategories();const category=categories.find(c=>c.slug===slug);if(!category) return notFound();const currentCategory=category;const products=await getProducts({category:slug});return <section className="py-10 sm:py-14"><div className="container-shell"><div className="mb-9 max-w-2xl"><p className="text-[10px] font-extrabold tracking-[.22em] text-[var(--brand-strong)]">COLLECTION</p><h1 className="mt-3 text-[40px] font-extrabold sm:text-[54px]">{currentCategory.name}</h1><p className="mt-3 text-sm leading-7 text-[var(--muted)]">{currentCategory.description}</p></div><ProductGrid products={products} categories={categories}/></div></section>}
