import type {Metadata} from "next";import Link from "next/link";import {ArrowUpLeft,Sparkles,Star} from "lucide-react";import {getCategories} from "@/lib/data";
export const metadata:Metadata={title:"المجموعات",description:"تسوّقي عبايات نوفا مودا حسب المجموعة والستايل."};
export default async function CollectionsPage(){const categories=(await getCategories()).filter(c=>c.slug!=="all");return <section className="py-10 sm:py-14"><div className="container-shell"><div className="mb-9 max-w-2xl"><p className="text-[10px] font-extrabold tracking-[.22em] text-[var(--brand-strong)]">SHOP BY MOOD</p><h1 className="mt-3 text-[38px] font-extrabold sm:text-[52px]">المجموعات</h1><p className="mt-3 text-sm leading-7 text-[var(--muted)]">بدل ما تضيع القطع بصفحة وحدة، كل ستايل إله مدخل مستقل.</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><Link href="/collections/new" className="collection-card bg-[var(--ink)] text-[var(--paper)]"><Sparkles/><div><h2>وصل حديثاً</h2><p>آخر الإضافات</p></div><ArrowUpLeft/></Link><Link href="/collections/featured" className="collection-card bg-[var(--surface-soft)]"><Star/><div><h2>مختاراتنا</h2><p>قطع نرشحها لك</p></div><ArrowUpLeft/></Link>{categories.map(c=><Link key={c.id} href={`/collections/${c.slug}`} className="collection-card bg-[var(--surface)]"><span className="text-2xl">✦</span><div><h2>{c.name}</h2><p>{c.description||"تصفحي المجموعة"}</p></div><ArrowUpLeft/></Link>)}</div></div></section>}
