import type {Metadata} from "next";import {Clock3,MapPin,Truck} from "lucide-react";import {getSettings} from "@/lib/data";import {formatPrice} from "@/lib/utils";
export const metadata:Metadata={title:"الشحن والتوصيل",description:"معلومات توصيل طلبات نوفا مودا داخل الأردن."};
export default async function ShippingPage(){const s=await getSettings();return <InfoPage eyebrow="DELIVERY" title="التوصيل… واضح من البداية." intro={`رسوم التوصيل الحالية ${formatPrice(s.delivery_price)} لجميع المحافظات، والمدة المعتادة 24–48 ساعة حسب المنطقة وتوفر الطلب.`} cards={[{icon:Truck,title:"رسوم التوصيل",text:`${formatPrice(s.delivery_price)} لجميع محافظات الأردن`},{icon:Clock3,title:"المدة المعتادة",text:"24–48 ساعة بعد تأكيد الطلب"},{icon:MapPin,title:"التغطية",text:"عمّان وإربد والزرقاء وباقي المحافظات"}]}/>}
function InfoPage({eyebrow,title,intro,cards}:any){return <section className="py-10 sm:py-14"><div className="container-shell"><div className="mx-auto max-w-3xl text-center"><p className="text-[10px] font-extrabold tracking-[.22em] text-[var(--brand-strong)]">{eyebrow}</p><h1 className="mt-3 text-[40px] font-extrabold sm:text-[54px]">{title}</h1><p className="mt-4 text-sm leading-8 text-[var(--muted)]">{intro}</p></div><div className="mx-auto mt-10 grid max-w-4xl gap-4 md:grid-cols-3">{cards.map(({icon:Icon,title,text}:any)=><div key={title} className="rounded-[26px] border border-[var(--line)] bg-[var(--surface)] p-6"><div className="grid h-11 w-11 place-items-center rounded-full bg-[var(--brand-soft)] text-[var(--brand-strong)]"><Icon size={18}/></div><h2 className="mt-5 font-extrabold">{title}</h2><p className="mt-2 text-xs leading-6 text-[var(--muted)]">{text}</p></div>)}</div></div></section>}
