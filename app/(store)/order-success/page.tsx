import type { Metadata } from "next";
import Link from "next/link";
import { CheckCircle2 } from "lucide-react";
export const metadata: Metadata = { title: "تم استلام الطلب", robots: { index:false, follow:false } };
export default async function OrderSuccess({ searchParams }: { searchParams: Promise<{ order?: string; demo?: string }> }) {const {order,demo}=await searchParams;return <section className="py-16 sm:py-24"><div className="container-shell"><div className="mx-auto max-w-xl rounded-[30px] border border-[#E7D8CC] bg-white p-7 text-center card-shadow sm:p-10"><CheckCircle2 className="mx-auto text-[#2E7D57]" size={56}/><h1 className="mt-5 text-3xl font-extrabold">تم استلام طلبك</h1><p className="mt-3 text-sm leading-8 text-[#78645B]">رقم الطلب: <strong>{order || "-"}</strong></p><p className="mt-2 text-sm leading-7 text-[#78645B]">سنراجع التفاصيل ونتواصل معك لتأكيد الطلب والتوصيل.</p>{demo && <p className="mt-4 rounded-xl bg-amber-50 p-3 text-xs leading-6 text-amber-900">هذا طلب تجريبي لأن قاعدة البيانات لم تُربط بعد.</p>}<div className="mt-7 flex flex-col gap-3 sm:flex-row sm:justify-center"><Link href="/shop" className="rounded-full bg-[#3D2B24] px-6 py-3 text-sm font-extrabold text-white">العودة للمتجر</Link><Link href="/" className="rounded-full border border-[#DCCABD] px-6 py-3 text-sm font-extrabold">الرئيسية</Link></div></div></div></section>}
