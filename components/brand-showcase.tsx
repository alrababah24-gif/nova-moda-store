"use client";
import Image from "next/image";
import Link from "next/link";
import { ArrowUpLeft } from "lucide-react";
import { motion,useReducedMotion } from "motion/react";
import type { Brand } from "@/lib/types";

export function BrandShowcase({brands}:{brands:Brand[]}){const reduce=useReducedMotion();return <section className="relative overflow-hidden py-10 sm:py-14"><div className="container-shell"><div className="mb-6 flex items-end justify-between gap-4"><div><p className="text-[10px] font-extrabold tracking-[.22em] text-[var(--brand-strong)]">BRANDS FIRST</p><h2 className="mt-2 text-[28px] font-extrabold sm:text-[36px]">اكتشفي البراندات</h2><p className="mt-2 max-w-xl text-sm leading-7 text-[var(--muted)]">كل خط له شخصية مختلفة، ومن لوحة الإدارة تقدري تضيفي وتعدلي أي براند.</p></div><Link href="/brands" className="hidden items-center gap-2 text-xs font-extrabold sm:flex">كل البراندات <ArrowUpLeft size={15}/></Link></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{brands.slice(0,4).map((brand,index)=><motion.div key={brand.id} initial={reduce?false:{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.25}} transition={{duration:.45,delay:index*.05}}><Link href={`/brands/${brand.slug}`} className="brand-tile group relative block min-h-[230px] overflow-hidden rounded-[26px] border border-[var(--line)] bg-[var(--surface-soft)]"><div className="absolute inset-0">{brand.cover_image&&<Image src={brand.cover_image} alt={brand.name} fill className="object-cover opacity-35 grayscale-[.2] transition duration-700 group-hover:scale-105 group-hover:opacity-45" sizes="(max-width:768px) 50vw,25vw"/>}<div className="absolute inset-0 bg-gradient-to-t from-[var(--surface)] via-[var(--surface)]/75 to-transparent"/></div><div className="relative flex min-h-[230px] flex-col justify-end p-5"><div className="mb-auto flex h-10 items-center">{brand.logo_url?<Image src={brand.logo_url} alt="" width={120} height={40} className="max-h-9 w-auto object-contain"/>:<span className="text-[10px] font-extrabold tracking-[.18em] text-[var(--brand-strong)]">NOVA MODA EDIT</span>}</div><h3 className="text-xl font-extrabold">{brand.name}</h3><p className="mt-2 min-h-10 text-xs leading-5 text-[var(--muted)]">{brand.tagline}</p><span className="mt-4 inline-flex items-center gap-1 text-[11px] font-extrabold">اكتشفي المجموعة <ArrowUpLeft size={13}/></span></div></Link></motion.div>)}</div><Link href="/brands" className="mx-auto mt-5 flex w-fit items-center gap-2 rounded-full border border-[var(--line)] bg-[var(--surface)] px-5 py-2.5 text-xs font-extrabold sm:hidden">كل البراندات <ArrowUpLeft size={14}/></Link></div></section>}
